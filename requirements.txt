fastapi==0.111.0
uvicorn==0.29.0
pydantic==2.7.1
pytest==8.2.0
openai==1.30.1
python-dotenv==1.0.1
requests==2.32.3
httpx==0.27.0
# openenv-core  # Commenting out if not strictly used by code, but keep if needed for validation
